{% extends "layouts/auth.njk" %}

{% block title %}Platform Dashboard{% endblock %}

{% block content %}
<div class="flex items-center justify-center min-h-screen">
    <div class="glow-effect top-1/4 left-1/4" style="animation: float 10s infinite ease-in-out;"></div>
    <div class="glow-effect bottom-1/4 right-1/4" style="animation: float 8s infinite ease-in-out reverse;"></div>

    <main class="relative z-10 text-center px-6">
        <div class="mb-4">
            <span class="text-sm font-black tracking-[0.4em] uppercase text-(--color-error) opacity-90">Acceso
                Denegado</span>
            <h1 class="error-code select-none" style="background: linear-gradient(to bottom, var(--color-error), #8b0000); background-clip: text; -webkit-background-clip: text; -webkit-text-fill-color: transparent;">403</h1>
        </div>

        <div class="max-w-md mx-auto">
            <h2 class="text-2xl md:text-3xl font-black mb-4 text-white uppercase tracking-tight">No autorizado</h2>
            <p class="text-(--text-muted) mb-10 leading-relaxed font-medium">
                Tu cuenta se encuentra suspendida o no cuentas con los permisos necesarios para acceder a esta página
            </p>

            <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                <a href="/"
                    class="bg-(--accent) hover:bg-(--accent-hover) text-white px-7 py-5 rounded-2xl text-base font-bold flex items-center justify-center gap-2 transition-all shadow-xl shadow-orange-900/20 active:scale-95 cursor-pointer">
                    Volver al Inicio
                </a>
            </div>
        </div>
    </main>
</div>
{% include "components/dashboard-footer.njk" %}

{% endblock %}