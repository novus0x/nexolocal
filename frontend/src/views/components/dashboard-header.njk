<div class="flex-1 flex flex-col min-w-0 overflow-hidden">
    <header
        class="h-20 glass-header border-b border-[#30363D] flex items-center justify-between px-8 shrink-0 z-40">
        <div class="flex items-center gap-6">
            <button onclick="toggle_sidebar()"
                class="p-2.5 hover:bg-[#21262D] rounded-xl text-gray-400 transition-colors cursor-pointer">
                <i class="fa-solid fa-bars text-2xl"></i>
            </button>
            <div class="hidden md:block">
                <h2 class="text-xl font-bold text-white leading-tight capitalize">Bienvenido {{user.username}}</h2>
                <p class="text-[11px] text-gray-500 font-medium">Gestiona tu negocio en tiempo real.
                </p>
            </div>
        </div>

        {# <div class="flex items-center gap-4">
            
        </div> #}
    </header>